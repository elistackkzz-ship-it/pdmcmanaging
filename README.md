Product Design and Manufacturing Consulting — Export
===================================================

This is a universal Next.js project export for **Product Design and Manufacturing Consulting**.

Business info included:
- Business name: Product Design and Manufacturing Consulting
- Address: 327 16th St
- Phone: (917) 572-5163
- Domain (meta): www.pdmcmanaging.com

How to run locally
------------------
1. Install dependencies:
   npm install

2. Run the development server:
   npm run dev

3. Open http://localhost:3000 in your browser.

How to deploy
-------------
- **Vercel** (recommended):
  1. Create a Vercel account and install the Vercel CLI or connect your GitHub repo.
  2. Push this project to a Git repository and import it in Vercel.
  3. Vercel will build and deploy automatically.

- **Netlify**:
  1. Build the site locally: `npm run build`
  2. Deploy the `.next` build output via methods described in Netlify docs, or use `next-on-netlify` adapter if needed.

Notes
-----
- This export uses plain CSS (no Tailwind) and is intentionally dependency-light so it's portable.
- If you'd like a Tailwind / more advanced version, I can generate that as well.